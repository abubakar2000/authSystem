/**
 * This component will be responsible for showing the 
 *  Details of a use account
 *  This will also show the update account information
 *  THis will also show the delete account functionality in a button 
 */
import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class AccountInfo extends Component {
    static propTypes = {
        prop: PropTypes
    }

    render() {
        return (
            <div>
                
            </div>
        )
    }
}
